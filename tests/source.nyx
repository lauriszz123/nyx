struct Monitor
  location: ptr;
end

impl Monitor
  fn new(location: ptr)
    self.location = location;
  end
end

struct Core
  monitor: Monitor;
end

impl Core
  fn new()
    self.monitor = new Monitor(0x5000);
  end

  fn putChar(chr: u8)
    poke(self.monitor.location, chr);
  end
end
